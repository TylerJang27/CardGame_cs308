package ooga.apiexamples;

public class StyleGui{
  private Collection<String> myStyleElements
  private Layout myLayout;
  private ExternalAPI view

  for(String style : myStyleElements){
    myView.setStyle(style)
  }
  myView.setLayout(myLayout);
  myView.displayStartupScreen();
}